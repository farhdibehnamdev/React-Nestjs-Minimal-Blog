export const URLS = {
  tagUrl: "api/tag/",
  categoryUrl: "api/category/",
  postUrl: "api/article/",
  userUrl: "api/auth/users",
  messageUrl: "api/message",
};

export type paginationOptionType = {
  offset: number;
  limit: number;
};

// export enum messageType {
//   sentMessage,
//   receivedMessage,
// }
